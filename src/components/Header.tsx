import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "left",
        alignItems: "left",
        height: "10vh",
        width: "100vw",
      }}
    >
      <div>Header</div>
    </Box>
  );
};

export default Header;
